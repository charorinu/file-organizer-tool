#ファイルやフォルダを操作のためosモジュールを準備

import os

#shutil ファイルやフォルダのコピー、移動、削除などの高レベルなファイル操作に特化したプロの道具箱の名称
#import プロの道具箱を自分のプログラムで使えるように「取り出す」または「準備する」という命令
#=ファイルをある場所から別の場所に移動する

import shutil

#ユーザーにフォルダのパスを入力してもらう
#input()関数を使うとプログラムの実行中にキーボードからの入力が受けられる
#f-string (f"...") を使うと変数や文字列の中に直接埋め込める。

print(f"\n監視フォルダ: {target_folder}")
print(f"\n移動ベースフォルダ: {destination_base_folder}")

#1行改行→監視フォルダ: (あなたが入力した監視フォルダのパス)」と画面に表示
#1行改行→移動ベースフォルダ: (あなたが入力した移動先ベースフォルダのパス)」と画面に表示

#フォルダが存在するか確認

if not os.path.exists(target_folder):
    #os.path.exists()  指定されたパス（target_folderの中身）があなたのコンピューター上に存在するかどうかを確認。
    #存在すれば True、存在しなければ False 
    #not もし target_folder が存在しないなら

    print("エラー:　指定された監視フォルダが見つかりません。パスを確認してください。 ")
    #もし監視フォルダが見つからなかった場合（if の条件が True ）エラーメッセージが画面に表示されます
    #ユーザーに何が問題なのかを知らせる大切なメッセージ

    print("プログラムを終了します。　")
    #エラーで停止しない場合これ以降の処理に進むのは意味がないため、ユーザーにここで処理は中断されることを伝えている
    #厳密に停止させたい場合は exit() を使うこともある

    #プログラムをここで停止させたい場合にこの行を追加

elif not os.isdir(target_folder):
    #target_folderがファイルだった場合のエラー

    print("エラー: 監視フォルダとして指定されたパスはフォルダではありません")
    #より詳細なエラーメッセージ

    print("プログラムを終了します。")
    #プログラムをここで停止させたい場合この行を追加

else: 
    #target_folderが存在し、かつフォルダであった場合　<--- ここに'else:'が必要です

    #移動先のベースフォルダが存在しない場合、作成
    #ここから以下の行が、上の'else:'に4つ分のスペースで字下げされます

    os.makedirs(destination_base_folder, exist_ok=True)

    if not os.path.isdir(destination_base_folder):
        #作成できなかった場合（権限など）

        print(f"エラー: 移動先ベースフォルダ　'{destination_base_folder}'を作成できませんでした。")
        print("プログラムを終了します。")
        #プログラムをここで停止させたい場合にこの行を追加

    else:
            #destination_base_folder が正常に存在または作成した場合
            #'pass'を書きエラーが出ないようにする

            pass #仮置き。　あとで実際の処理を記述

print("\n--- 処理が完了しました　---")        


    

